<UserControl x:Class="PV_Calculate.View.Maps"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation" 
             xmlns:local="clr-namespace:PV_Calculate.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Column="0" Grid.Row="1" Margin="10 0" Background="White">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

                <StackPanel Margin="10,0">
                    <Label Content="Latitude"/>
                    <TextBox x:Name="txtLat" Width="110" Text="36.6295" Style="{StaticResource RoundedTextBoxStyle}"/>
                </StackPanel>

                <StackPanel Margin="10 10 10 10">
                    <Label Content="Longitude"/>
                    <TextBox x:Name="txtLon" Width="110" Text="3.0058" Style="{StaticResource RoundedTextBoxStyle}"/>
                </StackPanel>
                <Button Content="Envoyer"   
                    Click="BtnGetDa_Click" 
                    Margin="0,0,00,0"
                    Width="102" Background="#FF82F5B3"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1" Margin="10 0">
            <gmap:GMapControl x:Name="MainMap" MouseRightButtonDown="gMapControl_MouseLeftButtonDown"/>
        </Grid>
    </Grid>
</UserControl>
